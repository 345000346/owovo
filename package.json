{
  "name": "owovo-blog",
  "version": "1.0.0",
  "description": "Personal blog powered by Hugo",
  "scripts": {
    "dev": "node ./scripts/run-hugo.mjs server -D",
    "build:site": "node ./scripts/run-hugo.mjs --gc --minify",
    "build:search": "pagefind --site public",
    "build": "npm run build:site && npm run build:search",
    "hugo:env": "node ./scripts/run-hugo.mjs env",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "keywords": [
    "hugo",
    "blog"
  ],
  "author": "LIGT",
  "license": "MIT",
  "engines": {
    "node": ">=24"
  },
  "browserslist": [
    "> 0.5% in CN",
    "last 2 versions",
    "not dead"
  ],
  "devDependencies": {
    "autoprefixer": "^10.4.27",
    "pagefind": "^1.5.2",
    "postcss": "^8.5.8",
    "prettier": "^3.8.3",
    "prettier-plugin-go-template": "^0.0.15",
    "sass-embedded": "^1.99.0"
  }
}
